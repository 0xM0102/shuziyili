# 构建管理后台时注入（编译期），勿当作服务器运行时 .env
# 与主站同域反代 /api/ 时（推荐，免 api 子域证书）：
#   VITE_API_BASE_URL=https://shuziyili.com npm run build
# 独立 API 子域时：
#   VITE_API_BASE_URL=https://api.shuziyili.com npm run build

VITE_API_BASE_URL=https://shuziyili.com
VITE_SITE_BASE_URL=https://shuziyili.com
